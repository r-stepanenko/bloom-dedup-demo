# Фильтр Блума

CLI-утилита на Go для демонстрации точной и приблизительной дедупликации событий по `event_hash`.  
Проект показывает разницу между точной дедупликацией через `map` и вероятностной дедупликацией через фильтр Блума, включая оценку ложных срабатываний, памяти и скорости.

## Назначение проекта
Утилита решает задачу по обработке потока событий в формате JSONL.  
Основная цель - сравнить:
- точную дедупликацию через `map`;
- приблизительную дедупликацию через Bloom Filter;
- стоимость этих подходов по памяти и производительности.

## Возможности
Проект поддерживает следующие сценарии:
- генерация тестового потока событий;
- чтение входного JSONL-файла построчно;
- точная дедупликация по `event_hash`;
- формирование отчёта по обработке;
- запуск контрольного demo-набора через `make demo`;
- запуск unit-тестов через `make test`;
- запуск benchmark-тестов через `make bench`.

## Сборка и запуск
Сборка проекта:

```powershell
make build
```
Запуск unit-тестов:

```powershell
make test
```
Запуск benchmark-тестов:

```powershell
make bench
```
Запуск контрольного demo:

```powershell
make demo
```

## CLI-команды

### Генерация тестового набора
Пример команды генерации событий:
```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe generate --count 1000000 --duplicate-ratio 0.05 --out ids.jsonl --seed 42 --sources 10
```

Параметры генератора:
- `--count` - число записей, целое число больше 0;
- `--duplicate-ratio` - доля дублей, от 0 до 0.9;
- `--out` - путь к выходному файлу;
- `--seed` - seed генератора;
- `--sources` - число источников, от 1 до 99 (допустимые источники - collector_01 ... collector_99).

### Запуск создания отчёта
Пример команды для запуска и сохранения отчёта в JSON и MD форматах:
```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe run --in ids.jsonl --config bloom.json --out report.json --report report.md
```

Команда:
- читает входной JSONL-файл;
- выполняет дедупликацию по `event_hash`;
- сохраняет JSON-отчёт в файл `--out`;
- при указании `--report` дополнительно сохраняет Markdown-отчёт;
- при `--exact-compare=false` файл обрабатывается потоково за один проход, точное сравнение через map не выполняется, в отчёте эти поля null.

### Benchmark
Пример команды для проведения benchmark тестирования:
```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe bench --in ids.jsonl --config bloom.json
```

Команда предназначена для benchmark-сценария и должна использоваться вместе с тестами производительности проекта.

### Сравнение false_positive_rate
Пример команды для проведения отчёта с таблицей разных `false_positive_rate`:

```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe fptable --in .\testdata\tests\ids.jsonl --rates 0.1,0.05,0.01,0.001 --hash f64
```
Пример выходных данных:

```powershell
Сравнение значений false_positive_rate

Записей: 1000000, уникальных (map): 950227, дублей (map): 49773, expected_items: 1000000
p     m (бит)      k     Память (байт)   Дубли (Блум)    Ложные срабатывания Реальный FP rate
|-----|-------|-----|----------|---------|----------|----------|
| 0.1 | 4792530 | 3 | 599067 | 82551 | 32778 | 0.034495 |
| 0.05 | 6235225 | 4 | 779404 | 59839 | 10066 | 0.010593 |
| 0.01 | 9585059 | 7 | 1198133 | 51001 | 1228 | 0.001292 |
| 0.001 | 14377588 | 10 | 1797199 | 52106 | 2333 | 0.002455 |
```

Данная команда выводит таблицу, в которой указаны параметры:
- m, 
- k, 
- память в байтах, 
- возможные дубли по Блуму,
- ложные срабатывания фильтра Блума,
- реальных false_positive

Все эти параметры расчитываются относительно `p` (false_positive_rate),
указанные в CLI команде через запятую.

Данная команда показывает, что `p` и реальный `FP` одновременно растут и падают: чем больше `p`, тем больше ложных срабатываний.

Та же таблица включается в JSON-отчёт (поле `fp_table`) и в Markdown-отчёт для стандартного набора rates (0.1, 0.05, 0.01, 0.001). В режиме `--exact-compare=false` в ней заполняются только расчётные параметры (m, k, память), а измеренные поля равны `null`.

## Формат входных данных

Основной входной файл - JSONL, одна JSON-запись на строку.  
Пример строки:

```json
{"seq":1,"event_id":"evt_000001","event_hash":"b9c950640e1b3740","source":"collector_01","timestamp":"2026-06-16T10:15:00Z"}
```

Описание полей:

- `seq` - порядковый номер записи;
- `event_id` - логический идентификатор события;
- `event_hash` - ключ дедупликации;
- `source` - источник события;
- `timestamp` - время события в формате RFC3339.

## Формат конфигурации

Конфигурация фильтра Блума задаётся JSON-файлом.  
Пример:

```json
{
  "expected_items": 1000000,
  "false_positive_rate": 0.01,
  "hash_family": "fnv64_double_hashing",
  "mode": "bloom"
}
```

Поля конфигурации:

- `expected_items` - ожидаемое число уникальных элементов;
- `false_positive_rate` - желаемая вероятность ложного срабатывания;
- `hash_family` - способ получения хеш-функций;
- `mode` - режим работы фильтра.

## Формат выходных данных

Основной JSON-отчёт идёт в --out, дополнительный Markdown в --report

Пример полей отчёта:

- `total_records`;
- `exact_unique`;
- `exact_duplicates`;
- `bloom_new`;
- `bloom_may_duplicate`;
- `estimated_false_positives`;
- `real_false_positive_rate`;
- `bloom_memory_bytes`;
- `exact_map_memory_bytes`;
- `duration_ms`;
- `map_duration_ms`;
- `bloom_duration_ms`;
- `fp_table` - сравнительная таблица для нескольких false_positive_rate;
- `by_source` - статистика по источникам (точные поля `null` в режиме no-map);
- `invalid_sources`.

Память измеряется по одинаковой логике: для фильтра Блума - размер битового массива в байтах, для map - оценка размера хранимых данных (ключи, поля событий).

## Алгоритм

Текущая реализация строится вокруг двух подходов:

1. **Точная дедупликация** через `map`, которая позволяет точно определить число уникальных записей и дублей.
2. **Приблизительная дедупликация** через фильтр Блума, который экономит память, но может давать ложные срабатывания.

Фильтр Блума использует:
- битовый массив на `[]uint64`;
- операции установки и проверки битов;
- расчёт параметров `m` и `k` из `expected_items` и `false_positive_rate`.

Фильтр Блума может сказать точно, нет ли элемента в списке, но сказать точно ,что он там есть, он не может.
Основной частью фильтра является хеширование его уникальных данных в число (индекс битового массива), и так как массив ограничен, а входных элементов может быть много, то могут возникать коллизии.
То есть на несколько разных элементов хеш даст один индекс. Поэтому если элемент помечен "1" (присутствием), то это не значит, что эта единица предназначалась именно **этому** элементу, а не его "коллизии".
Точной информацией будет, если элемент помечен "0" (отсутствием), что означает, что ни этот, ни любой другой элемент с данным индексом ещё не попадался. Именно поэтому бывают ложные срабатывания.

Почему фильтр Блума экономит память?
map хранит полные хеши (16-32 байта каждый) + служебную информацию;
Фильтр Блума хранит только битовый массив фиксированного размера;

## Известные ограничения

Текущая версия проекта имеет следующие ограничения:

- основная дедупликация выполняется глобально по `event_hash`;
- при превышении expected_items реальная вероятность ложных срабатываний растёт;
- максимальный размер входного файла ограничен доступной памятью (читается построчно);
- в быстром режиме без точного сравнения некоторые метрики ложных срабатываний могут быть недоступны.


## Характеристики машины, на которой запускались тесты

Тесты и бенчмарки выполнялись на следующей конфигурации:

**ОС**	Windows 11 IoT Корпоративная LTSC (24H2, сборка 26100.8655)

**Процессор**	AMD Ryzen 7 6800H with Radeon Graphics (3.20 GHz)

**ОЗУ**	32.0 ГБ (доступно 27.8 ГБ)

**Архитектура**	64-разрядная, процессор x64

**Go** версия	1.21+

Абсолютные значения времени и скорости в примерах ниже зависят от машины и могут отличаться.

## Контрольный запуск

Для быстрой проверки используется фиксированный набор в `testdata/control`.
Команда запуска:

```powershell
make demo
```

После выполнения создаются файлы:
- `output/demo_report.json`
- `output/demo_report.md`

Краткий результат контрольного запуска (полный отчёт также содержит таблицу `fp_table`):
- `total_records`: 30
- `bad_lines`: 0
- `exact_unique`: 22
- `exact_duplicates`: 8
- `bloom_new`: 22
- `bloom_may_duplicate`: 8
- `estimated_false_positives`: 0
- `real_false_positive_rate`: 0

Статистика по источникам:
- `collector_01`: 13 записей, 11 уникальных, 2 дубля;
- `collector_02`: 6 записей, 5 уникальных, 1 дубль;
- `collector_03`: 11 записей, 10 уникальных, 1 дубль.

Некорректные источники:
- отсутствуют (`invalid_sources = []`).

## Проверка тестов
Команда:

```powershell
make test
```

Результат:
- `internal/bitset` - ok
- `internal/bloom` - ok
- `internal/model` - ok
- `internal/report` - ok

## Запуск анализа фильтров
Команда:

```powershell
make bench
```

Результат:
```--- BENCH ---

Входной файл:                testdata/control/demo_events.jsonl
Файл конфигурации:           testdata/control/demo_config.json
Флаг пропуска:               true

--- Метрики ---

Метрика                                  Точное сравнение   Фильтр Блума
---------------------------------------- ------------------ ------------------
Уникальные                               22                 22
Дубликаты                                8                  8
Ложные срабатывания                      0                  0
Ложное срабатывание rate                 0.0000000000       0.0000000000
Память, байт                             2508               1199
Память, Мб                               0.00               0.00
Время, мс                                0                  0
Строк в секунду                          0.00               0.00

Превышение памяти map над bloom:         2.09 раза
Замедление map относительно bloom:       0.00 раза
```

## Запуск на больших данных
Для входного файла с 1000000 записей должно быть использовано не более 4 Мб и должно обрабатываться не менее 500 000 ID/с

Для генерации файла можно использовать команду:

```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe generate --count 1000000 --duplicate-ratio 0.05 --out .\testdata\tests\ids.jsonl --seed 42 --sources 10
```

Файл конфигурации можно использовать из папки tests с парамтерами:

```json
{
   "expected_items": 1000000,
   "false_positive_rate": 0.01,
   "hash_family": "fnv64_double_hashing",
   "mode": "bloom"
 }
```

Команда генерации отчётов в быстром режиме no-map:
```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe run --in .\testdata\tests\ids.jsonl --config .\testdata\tests\bloom.json --out .\output\report_one_m.json --exact-compare=false
```

Команда запуска bench для просмотра характеристик фильтра:

```powershell
.\cmd\bloom-dedup-demo\bloom-dedup-demo.exe bench --in .\testdata\tests\ids.jsonl --config .\testdata\tests\bloom.json
```

Вывод команды:
```powershell
--- BENCH ---

Входной файл:                .\testdata\tests\ids.jsonl
Файл конфигурации:           .\testdata\tests\bloom.json
Флаг пропуска:               true

--- Метрики ---

Метрика                                  Точное сравнение   Фильтр Блума
---------------------------------------- ------------------ ------------------
Уникальные                               950227             948999
Дубликаты                                49773              51001
Ложные срабатывания                      0                  1228
Ложное срабатывание rate                 0.0000000000       0.0012923228
Память, байт                             108325879          1198133
Память, Мб                               103.31             1.14
Время, мс                                183                130
Строк в секунду                          5464480.87         7692307.69

Превышение памяти map над bloom:         90.41 раза
Замедление map относительно bloom:       1.41 раза
```

Результаты показывают, что память меньше 4 Мб, а обработка строк более 500 000 в секунду.